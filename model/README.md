## Example Model - Credit Scoring

### Instructions

Run the following commands in order to generate the data, train the model and export the onnx file
```
python generate.py
python train.py
python export_onnx.py
```